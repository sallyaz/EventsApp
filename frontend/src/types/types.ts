export interface FormState {
    [key: string]: string;
  }
  
  export interface ErrorState {
    [key: string]: string;
  }

  export interface User {
    id: string;
    name: string;
    email: string;
  }